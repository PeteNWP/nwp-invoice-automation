# Mock document previews

Place local supplier invoice PDFs here when testing the prototype.

Real client PDFs/emails are intentionally git-ignored and must not be committed.
The mock document store points preview URLs at this folder when matching files exist.

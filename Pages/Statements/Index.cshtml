@page
@model Nwp.InvoiceAutomation.Web.Pages.Statements.IndexModel

<div class="page-heading">
    <div>
        <p class="eyebrow">Queue</p>
        <h1>Statements</h1>
        <p class="muted">Supplier account statements, separated from invoices as Maeve requested — supplier, statement date, total due and the original document in one list.</p>
    </div>
    <div class="approval-summary">
        <strong>@Model.Documents.Count</strong>
        <span>statements</span>
    </div>
</div>

<partial name="_DocumentTable" model="Model.Documents" />
